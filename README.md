Process To Run :


In CIO_Event-VERSION_2_ROUTER_REALTIME_SYNC run :

 - npm install - To install node modules

In main folder ( final_CIO_Event) run :

-  npm init -y
👉 This creates a new package.json in root

- npm install concurrently
👉 This installs tool to run multiple commands

- npm start

Terminal output like:

[0] Vite running on http://localhost:5173
[1] Flask running on http://127.0.0.1:5001

👉 Both apps running together 🎉
